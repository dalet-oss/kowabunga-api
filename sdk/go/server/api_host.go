/*
 * Kowabunga API documentation
 *
 * Kvm Orchestrator With A BUNch of Goods Added
 *
 * API version: 0.33.0
 * Contact: csops@dalet.com
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package server

import (
	"encoding/json"
	"net/http"
	"strings"

	"github.com/gorilla/mux"
)

// HostAPIController binds http requests to an api service and writes the service results to the http response
type HostAPIController struct {
	service HostAPIServicer
	errorHandler ErrorHandler
}

// HostAPIOption for how the controller is set up.
type HostAPIOption func(*HostAPIController)

// WithHostAPIErrorHandler inject ErrorHandler into controller
func WithHostAPIErrorHandler(h ErrorHandler) HostAPIOption {
	return func(c *HostAPIController) {
		c.errorHandler = h
	}
}

// NewHostAPIController creates a default api controller
func NewHostAPIController(s HostAPIServicer, opts ...HostAPIOption) Router {
	controller := &HostAPIController{
		service:      s,
		errorHandler: DefaultErrorHandler,
	}

	for _, opt := range opts {
		opt(controller)
	}

	return controller
}

// Routes returns all the api routes for the HostAPIController
func (c *HostAPIController) Routes() Routes {
	return Routes{
		"DeleteHost": Route{
			strings.ToUpper("Delete"),
			"/api/v1/host/{hostId}",
			c.DeleteHost,
		},
		"ListHostInstances": Route{
			strings.ToUpper("Get"),
			"/api/v1/host/{hostId}/instances",
			c.ListHostInstances,
		},
		"ListHosts": Route{
			strings.ToUpper("Get"),
			"/api/v1/host",
			c.ListHosts,
		},
		"ReadHost": Route{
			strings.ToUpper("Get"),
			"/api/v1/host/{hostId}",
			c.ReadHost,
		},
		"ReadHostCaps": Route{
			strings.ToUpper("Get"),
			"/api/v1/host/{hostId}/caps",
			c.ReadHostCaps,
		},
		"UpdateHost": Route{
			strings.ToUpper("Put"),
			"/api/v1/host/{hostId}",
			c.UpdateHost,
		},
	}
}

// DeleteHost - 
func (c *HostAPIController) DeleteHost(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	hostIdParam := params["hostId"]
	if hostIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"hostId"}, nil)
		return
	}
	result, err := c.service.DeleteHost(r.Context(), hostIdParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// ListHostInstances - 
func (c *HostAPIController) ListHostInstances(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	hostIdParam := params["hostId"]
	if hostIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"hostId"}, nil)
		return
	}
	result, err := c.service.ListHostInstances(r.Context(), hostIdParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// ListHosts - 
func (c *HostAPIController) ListHosts(w http.ResponseWriter, r *http.Request) {
	result, err := c.service.ListHosts(r.Context())
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// ReadHost - 
func (c *HostAPIController) ReadHost(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	hostIdParam := params["hostId"]
	if hostIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"hostId"}, nil)
		return
	}
	result, err := c.service.ReadHost(r.Context(), hostIdParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// ReadHostCaps - 
func (c *HostAPIController) ReadHostCaps(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	hostIdParam := params["hostId"]
	if hostIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"hostId"}, nil)
		return
	}
	result, err := c.service.ReadHostCaps(r.Context(), hostIdParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// UpdateHost - 
func (c *HostAPIController) UpdateHost(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	hostIdParam := params["hostId"]
	if hostIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"hostId"}, nil)
		return
	}
	hostParam := Host{}
	d := json.NewDecoder(r.Body)
	d.DisallowUnknownFields()
	if err := d.Decode(&hostParam); err != nil {
		c.errorHandler(w, r, &ParsingError{Err: err}, nil)
		return
	}
	if err := AssertHostRequired(hostParam); err != nil {
		c.errorHandler(w, r, err, nil)
		return
	}
	if err := AssertHostConstraints(hostParam); err != nil {
		c.errorHandler(w, r, err, nil)
		return
	}
	result, err := c.service.UpdateHost(r.Context(), hostIdParam, hostParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}
