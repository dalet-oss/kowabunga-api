components:
  schemas:
    ObjectIds:
      type: array
      items:
        type: string
  parameters:
    OptionalQueryNotify:
      name: notify
      in: query
      description: Whether or not to send a notification email at resource creation.
      schema:
        type: boolean
